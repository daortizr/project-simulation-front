# Optional. Leave unset in dev so the app calls `/api` on the Vite dev server (proxied to localhost:3000).
# Set for production builds when the API is on another origin (must allow CORS or use a gateway).
# VITE_API_BASE_URL=https://api.example.com
